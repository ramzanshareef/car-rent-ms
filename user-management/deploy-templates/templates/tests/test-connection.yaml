apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "user-management.fullname" . }}-test-connection"
  labels:
    {{- include "user-management.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "user-management.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
