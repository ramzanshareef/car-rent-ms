apiVersion: v1
kind: Service
metadata:
  name: {{ include "home-service-management.fullname" . }}
  labels:
    {{- include "home-service-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "home-service-management.selectorLabels" . | nindent 4 }}
