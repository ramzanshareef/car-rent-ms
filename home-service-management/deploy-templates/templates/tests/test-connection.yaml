apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "home-service-management.fullname" . }}-test-connection"
  labels:
    {{- include "home-service-management.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "home-service-management.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
