apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "car-management.fullname" . }}-test-connection"
  labels:
    {{- include "car-management.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "car-management.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
